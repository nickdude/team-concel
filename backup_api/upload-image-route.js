// backup of original upload-image route
// Location: src/app/api/upload-image/route.js (moved out to avoid being treated as an app route)
import fs from 'fs';
import path from 'path';
// Original logic omitted here for brevity; full backup kept for manual restore.

// --- original code preserved for restore ---

/*
import { NextResponse } from 'next/server';
import fs from 'fs';
import path from 'path';

export async function POST(request) {
    ...
}

export async function GET() {
    ...
}
*/

export default 'backup';
